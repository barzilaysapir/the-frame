# Wrangler-only local secrets (read by `wrangler dev` / `next dev` via the
# OpenNext Cloudflare platform proxy, and by `npm run preview`). Unlike
# .env.local, these are never exposed to the browser — server-only.
#
# Copy this file to `.dev.vars` (gitignored) and fill in a real value.
# `.dev.vars` is NOT read in production — for that, set the same name as a
# Worker secret: `npx wrangler secret put VIDEO_SIGNING_SECRET`.

# Signs/verifies the short-lived playback URLs used to gate real (non-mock)
# external-course video lessons stored in the private R2 bucket
# `the-frame-class-videos` — see lib/server/course-videos.ts. Any long random
# string works; generate one with:
#   node -e "console.log(crypto.randomUUID() + crypto.randomUUID())"
VIDEO_SIGNING_SECRET=

# Gates the `/api/preview?token=...` link that unlocks the demo/mock catalog
# (routines, instructors) — hidden by default everywhere, including this
# local dev server, once migration 0027 is applied. See lib/preview.ts and
# app/api/preview/route.ts. Any long random string works (same generator as
# above). Leave unset to disable the preview link entirely.
PREVIEW_CATALOG_TOKEN=

# Comma-separated list of Firebase-verified emails allowed onto the admin
# purchases page (app/[locale]/admin/purchases) — the manual "mark Bit
# payment as paid/refunded" tool for Phase 1. See lib/server/admin.ts.
# Missing/empty means nobody is an admin, not "allow everyone".
ADMIN_EMAILS=

# Grow (Meshulam) payment gateway — see lib/server/payments/grow.ts and
# developers.grow.business. Get real values from Grow support after
# signing up; the identifiers in their docs are demo-only. Leave any of
# these three unset to keep the Phase-1 manual Bit flow (no redirect).
#
# userId identifies your business.
GROW_USER_ID=
# pageCode identifies which payment page/method to use — the "Generic
# Payment Page" accepts both card and Bit (see developers.grow.business/
# reference/payment-page). Grow assigns your real one; the doc-listed demo
# value is b73ca07591f8.
GROW_PAGE_CODE=
# Sandbox: https://sandbox.meshulam.co.il/api/light/server/1.0
# Production (only after Grow approves a production review — see
# developers.grow.business/reference/live-environment):
#   https://secure.meshulam.co.il/api/light/server/1.0
# IMPORTANT: Bit/Apple Pay/Google Pay have NO sandbox — those charge for
# real even when this points at the sandbox host. Only card-number testing
# (e.g. 4580458045804580) is safe pre-launch.
GROW_BASE_URL=https://sandbox.meshulam.co.il/api/light/server/1.0
