---
description: Sample teachers, songs, and name-bank entries are mocks only
alwaysApply: true
---

# Mock content

Teachers, songs, bios, Instagram URLs, posters, and `lib/name-bank.ts` are **mocks for UI/demo** until real production content is provided.

- Do not treat bank names as final brand/legal choices
- Song/artist pairs are placeholder catalog examples (clearance/licensing TBD)
- Instagram URLs may be placeholders — replace only with URLs the user supplies
- Prefer pulling new demo rows from `lib/name-bank.ts` instead of inventing one-off names
