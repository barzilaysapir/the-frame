/**
 * Site-wide monthly subscription (ILS).
 * Must stay above a single combination’s list price so all-access isn’t
 * cheaper than one routine.
 */
export const MONTHLY_SUBSCRIPTION_ILS = 249;

export type CheckoutPlanId = "rental" | "subscription";
